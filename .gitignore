.DS_Store

# APJSKIN working files (deletion is blocked from the cloud session)
mp3gem/mp3gem.c.pre-skin
icons/out/newicons.png
skins/_to_delete/

# harness binaries and their output (deletion is blocked from the
# cloud session, so they are ignored rather than removed)
tests/*/harness
tests/*/out.ppm

# build products and scratch that only exist because the cloud session
# cannot delete files in a connected folder
*.o
*.bak
*.dSYM/
_to_delete/
Claude outputs/
mp3gem/mp3gem.inf

# first cut of the PSPDF host change as a git patch; superseded by hostpatches/pspdf/
hostpatches/0021-PSPDF-host-side-PDF-rendering.patch
